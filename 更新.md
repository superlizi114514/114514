# 更新日志

## 2026-03-13

### 新增
- 人员身份选项：在人员列表添加人员处添加学生/老师选项
  - 默认选择「学生」
  - 选择「老师」时禁用年级和系输入框，自动归类到「00 级老师系」
  - 选择「学生」时正常填写年级和系

### 修复
- 票数扣减 bug（时区问题）：修复 UTC+8 时区与 SQLite `datetime()` 比较不匹配的问题
  - 在 `db-vercel.ts` 中添加 `'utc'` 修饰符到 `datetime(createdAt, 'utc')` 查询
  - 修复 `findProfileReviewsTodayByReviewer` 方法
  - 修复 `findMerchantReviewsTodayByReviewer` 方法
  - 修复 `countProfileReviewsByReviewerAndProfile` 方法
  - 修复 `countMerchantReviewsByReviewerAndMerchant` 方法
  - 统一 `merchants.ts` 中的时区处理为 UTC+8（与 `profiles.ts` 一致）

### 状态
- ✅ 票数扣减 bug 已修复（2026-03-13）
- ✅ 人员身份选项已添加（2026-03-13）

---

## 2026-03-12

### 修复
- 票数消耗问题：修复 D1 数据库时间比较格式，使用 `datetime(createdAt)` 和 `YYYY-MM-DD HH:MM:SS` 格式
- Logo 显示问题：修复 lvg.png 导入路径，样式改为 `object-fit: contain`
- 管理后台修改票数功能：添加 `/api/admin/profiles/reviews/:id/votes` 和 `/api/admin/merchants/reviews/:id/votes` 接口
- 赞助管理加载失败：修复 support.ts 使用 `c.env.DB.prepare()` 语法，改为使用 `db-vercel` 查询
- D1 SQLite 兼容性问题：修复 db-vercel.ts 中所有 `RETURNING *` 语法（SQLite 不支持）
  - 修复 `updateUser` 方法
  - 修复 `createUser` 方法
  - 修复 `createProfile` 方法
  - 修复 `updateProfile` 方法
  - 修复 `createProfileReview` 方法
  - 修复 `createMerchant` 方法
  - 修复 `updateMerchant` 方法
  - 修复 `createMerchantReview` 方法
  - 修复 `createPartTimeJob` 方法
  - 修复 `updatePartTimeJob` 方法
- 数据库字段缺失：为 `support_records` 表添加 `sponsorName` 字段
- 页面加载状态：为所有页面添加加载时旋转图标提示
  - `Profiles.vue`: 添加人员列表加载状态
  - `Merchants.vue`: 添加商家列表加载状态
  - `ProfileRankings.vue`: 添加人员榜单加载状态
  - `Support.vue`: 添加赞助者名单加载状态
  - `MerchantRankings.vue`: 已有加载状态
  - `PartTimeJobs.vue`: 已有加载状态
  - `ProfileReviews.vue`: 添加人员点评页加载状态
  - `MerchantReviews.vue`: 添加商家点评页加载状态

### 状态
- ✅ 票数消耗问题已确认修复
- ✅ Logo 显示已确认修复
- ✅ 管理后台修改票数功能已确认修复（2026-03-12）
- ✅ 赞助管理功能已修复（2026-03-12）
- ✅ D1 SQLite 兼容性已修复（2026-03-12）
- ✅ 数据库字段缺失已修复（2026-03-12）
- ✅ 页面加载状态已添加（2026-03-12）
